export type Data = {
    userId: number,
    id: number,
    title: string,
    body: string, 
};

export type Comments = {
    postId: number,
    id: number,
    name: string,
    email: string,
    body: string, 
};